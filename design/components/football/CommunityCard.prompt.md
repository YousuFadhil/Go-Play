A community in a list.

```jsx
<CommunityCard name="Al Shamal" description="Thursday and Sunday games in the north"
  memberCount={24} upcomingCount={2} role="Admin" onClick={open} />
```

The whole row opens the community. Add `trailing` only where a list offers a second action that is genuinely different from opening — "Join" on a community you are not in.
