A setting row with an explanation and a switch.

```jsx
<SwitchRow label="Match notifications" subtitle="Registration, player changes and reminders." checked onChange={set} />
```

Always write the subtitle: a switch whose effect is unstated is a switch nobody will touch.
