A hairline rule between grouped rows.

```jsx
<Divider />
<Divider inset={56} tight />
```

Used inside a card to separate rows, and once in the community action sheet to hold the destructive action apart from the rest.
