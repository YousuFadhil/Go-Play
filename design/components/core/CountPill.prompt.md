The count of a group, in a quiet pill at the end of its section heading.

```jsx
<SectionHeading title="Members" count={12} />
```

Use it only where the number is load-bearing. Discover's sections deliberately carry none: the cards below are the count.
