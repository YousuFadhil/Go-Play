The quiet last line of a screen — an explanation, never a warning.

```jsx
<FootNote>Reserve players: 6 (automatic) — maximum registration: 20</FootNote>
```

Outline colour, 12px. If the sentence is a failure, it belongs in an `ErrorState` or a `Snackbar` instead.
