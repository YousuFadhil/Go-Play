Announces a group of rows inside a Sheet.

```jsx
<SectionHeading title="Members" count={24} action="Manage" />
<SectionHeading title="Starting" count="6 / 12" action="Arrange" />
```

The count is inline after a middot, not in a pill: it reads left-to-right in both languages and costs no width.
