Nothing to show, and that is normal — a new community, a quiet week.

```jsx
<EmptyState icon="sports_soccer" tone="accent"
  message={"No upcoming matches.\nJoin a community to get started."} />
```

Never style it as a failure. An empty state whose button leads nowhere is worse than one without: it implies the emptiness is the reader's to fix.
