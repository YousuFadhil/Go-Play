A pulsing placeholder in the shape of the content that is loading.

```jsx
<Skeleton width={200} height={22} />
<Skeleton height={124} radius="var(--radius-md)" />
```

Use where the shape is known (a match card, a profile). Where it is not, use `LoadingState`.
