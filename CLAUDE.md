# Go Play — سياسة العمل الدائمة

هذا الملف يحدد قواعد العمل الثابتة لمشروع Go Play. الهدف: تقليل استهلاك التوكن، والحفاظ على التركيز على الـ MVP، وعدم تكرار قرارات/اختبارات معتمدة سابقاً.

> **خط الأساس الحالي:** `v0.5.1-mvp` — معمارية Community-first مع روابط الدعوة القابلة للمشاركة. المجتمع هو الجذر التجميعي، وكل صلاحية تُشتق من `community_members.role` عبر `has_community_role` (owner ≥ admin ≥ player)، ولا يُقرأ `owner_id` ولا `created_by` لمنح صلاحية. يشمل الدعوات وإدارة الأعضاء ونقل الملكية وحذف المجتمع، إضافة إلى V2 Match Management (دورة حياة مفتوحة/ممتلئة/مكتملة، قفل مشتق، سعة مشتقة من إعداد الاحتياط العالمي، حذف غير مرتبط بالوقت، إشعارات داخل التطبيق). ودعوة المجتمع/المباراة عبر رابط قابل للمشاركة، بمعاينة تعمل قبل إنشاء الحساب.
>
> راجع `CHANGELOG.md` لتفاصيل الإصدار، و`Docs/10-Design-Decisions.md` (DD-01 إلى DD-11) للقرارات المعتمدة، و`Docs/12-Testing.md` لتشغيل الاختبارات، و`Docs/11-Future-Backlog.md` للأفكار والفجوات المؤجلة — ومنها **مغادرة المجتمع بلا واجهة**: القاعدة (PD-12) منفَّذة ومختبَرة في قاعدة البيانات، وبقاؤها بلا واجهة قرار مقصود حتى الآن.

## 1. MVP First

- الأولوية دائماً لإطلاق وتحسين الـ MVP.
- لا تضف أي Feature أو Refactoring أو تحسين خارج نطاق المهمة الحالية إلا إذا طُلب ذلك صراحة.
- إذا كانت لديك أفكار أو تحسينات، وثّقها كاقتراحات مستقبلية في `Docs/11-Future-Backlog.md` ولا تنفذها.
- أي تطوير جديد (غير إصلاح أخطاء) يبدأ على فرع Feature منفصل — لا يُدمج مباشرة في `main`.

## 2. Product Owner Authority

- قرارات Product Owner هي المرجع النهائي.
- إذا أكد Product Owner أن ميزة أو إصلاحاً قد تم اختباره واعتماده، فلا تُعِد الاختبارات الوظيفية أو Smoke Tests أو التحقق اليدوي إلا إذا:
  - طلب Product Owner ذلك صراحة، أو
  - تم تعديل منطق لم يكن جزءاً من الاختبار السابق، أو
  - ظهرت مشكلة جديدة، أو
  - كان الدمج أو البناء يتطلب إعادة التحقق لسبب هندسي واضح.

## 3. Token Efficiency

اعمل دائماً على تقليل استهلاك التوكن. لا تكرر:
- التحليل الذي تم اعتماده.
- الشرح الذي تمت الموافقة عليه.
- الاختبارات التي تم اعتمادها.
- القرارات التي تم حسمها.

اعتمد على القرارات السابقة (بما فيها الذاكرة المحفوظة) بدلاً من إعادة مناقشتها.

## 4. Scope Discipline

نفّذ المهمة المطلوبة فقط. لا تنتقل إلى:
- تحسينات.
- تنظيف للكود.
- إعادة هيكلة.
- تحديثات للتوثيق.
- تحسينات للأداء.

إلا إذا كانت جزءاً من المهمة الحالية أو طُلبت صراحة.

## 5. Release Policy

إذا كانت المهمة تتعلق فقط بالدمج أو إنشاء إصدار:

- لا تُعِد تنفيذ الاختبارات التي اعتمدها Product Owner.
- نفّذ الحد الأدنى اللازم لإنتاج نسخة مستقرة.
- لا تضف خطوات إضافية لا تضيف قيمة مباشرة.

## 6. Communication Style

في نهاية كل مهمة قدّم فقط:

- ما الذي تم تنفيذه.
- ما هي الخطوة التالية.

ولا تكرر تفاصيل سبق توثيقها إلا إذا طُلب ذلك.
