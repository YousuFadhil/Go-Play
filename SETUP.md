# دليل الإعداد والتشغيل — Go Play (Sprints 1–4)

هذا الدليل يشرح الخطوات المطلوبة لتشغيل التطبيق لأول مرة.
خطوتان فقط تتطلبان تدخل مالك المنتج: إنشاء مشروع Supabase، وتثبيت Flutter SDK.

---

## 1. إنشاء مشروع Supabase (مطلوب — يقوم به مالك المنتج)

1. سجّل حساباً في <https://supabase.com> (الخطة المجانية كافية للمرحلة الحالية).
2. أنشئ مشروعاً جديداً باسم `go-play`.
3. من **Authentication → Providers → Phone**:
   - فعّل مزوّد Phone.
   - **عطّل خيار "Confirm phone"** (مهم: هذا يسمح بالتسجيل برقم الجوال وكلمة المرور بدون رسائل SMS وبدون أي تكلفة).
4. من **SQL Editor** نفّذ ملفات الهجرة بالترتيب:
   1. `supabase/migrations/0001_users.sql`
   2. `supabase/migrations/0002_groups.sql`
   3. `supabase/migrations/0003_matches.sql`
   4. `supabase/migrations/0004_match_registrations.sql`
5. من **Project Settings → API** انسخ القيمتين:
   - `Project URL`
   - `Publishable key` (أو `anon public key` في المشاريع الأقدم — كلاهما يعمل)

> ملاحظة أمنية: مفتاح `anon` مخصص للاستخدام في التطبيق وهو آمن للتضمين،
> أما مفتاح `service_role` فلا يُستخدم في التطبيق إطلاقاً.

---

## 2. Flutter SDK (مثبّت ✅)

Flutter 3.44.7 مثبّت في: `C:\Users\yousi\flutter`

خطوتان متبقيتان يدويتان:

1. أضف `C:\Users\yousi\flutter\bin` إلى متغير PATH الخاص بالمستخدم
   (Settings → System → About → Advanced system settings → Environment Variables).
2. لتشغيل التطبيق على الجوال: ثبّت Android Studio (لتوفير Android SDK والمحاكي)
   ثم نفّذ `flutter doctor` واتبع التعليمات.

ملاحظة: التحليل الثابت (`flutter analyze`) والاختبارات (`flutter test`)
تعمل الآن بدون Android Studio، وقد تم تشغيلها بنجاح.

---

## 3. توليد ملفات المنصات (مرة واحدة)

مجلد `app/` يحتوي كود المشروع فقط. لتوليد ملفات Android/iOS نفّذ داخل مجلد `app/`:

```
cd app
flutter create . --org com.goplay --project-name go_play --platforms android,ios
flutter pub get
```

---

## 4. تشغيل التطبيق

استبدل القيمتين بالقيم المنسوخة من Supabase:

```
flutter run ^
  --dart-define=SUPABASE_URL=https://xxxx.supabase.co ^
  --dart-define=SUPABASE_ANON_KEY=eyJ...
```

---

## 5. تشغيل الاختبارات

```
cd app
flutter test
```

---

## 6. اختبار يدوي سريع (Sprint 1)

1. افتح التطبيق → تظهر شاشة تسجيل الدخول.
2. اضغط «إنشاء حساب» وأدخل: الاسم، رقم الجوال بصيغة دولية (مثال `9665xxxxxxxx`)، كلمة مرور (8 أحرف فأكثر)، والمركز الأساسي.
3. بعد النجاح تظهر الشاشة الرئيسية مباشرة.
4. تحقق في Supabase (**Table Editor → users**) من إنشاء صف للمستخدم تلقائياً.
5. سجّل الخروج ثم سجّل الدخول بنفس الرقم وكلمة المرور.
6. جرّب إنشاء حساب بنفس الرقم مرة أخرى → تظهر رسالة «رقم الجوال مسجل مسبقاً».

---

## 7. اختبار يدوي سريع (Sprint 2: المجموعات)

1. من الشريط السفلي افتح تبويب «المجموعات».
2. أنشئ مجموعة جديدة (جرّب عامة ثم خاصة) → تظهر في قائمة مجموعاتك.
3. افتح تفاصيل المجموعة → يظهر رمز الانضمام وقائمة الأعضاء وأنت فيها بشارة «المالك».
4. انسخ رمز الانضمام.
5. بحساب مستخدم آخر، اضغط أيقونة المفتاح في شاشة المجموعات وأدخل الرمز → ينضم ويظهر في قائمة الأعضاء.
6. جرّب إدخال رمز خاطئ → «لا توجد مجموعة بهذا الرمز»، ورمز مجموعة أنت عضو فيها → «أنت عضو في هذه المجموعة بالفعل».

---

## 8. اختبار يدوي سريع (Sprint 3: المباريات)

1. افتح تفاصيل مجموعة → يظهر تبويبا «المباريات» و«الأعضاء».
2. اضغط زر الإضافة وأنشئ مباراة: الموقع، التاريخ، وقت البداية والنهاية، والحد الأقصى للاعبين.
3. جرّب وقت نهاية قبل البداية أو تاريخاً ماضياً → تظهر رسالة الخطأ المناسبة.
4. المباراة الجديدة تظهر في تبويب مباريات المجموعة وفي «المباريات القادمة» بالشاشة الرئيسية.
5. افتح تفاصيل المباراة بحساب المنشئ → يظهر زر «إلغاء المباراة»؛ ألغِ المباراة وأكّد → تظهر بحالة «ملغاة» وتختفي من القادمة.
6. افتح نفس المباراة بحساب عضو آخر → لا يظهر زر الإلغاء.
7. تحقق أن مستخدماً خارج المجموعة لا يرى مبارياتها إطلاقاً.

---

## 9. اختبار يدوي سريع (Sprint 4: التسجيل والاحتياط)

المطلوب: 3 حسابات على الأقل في مجموعة واحدة، ومباراة بحد أقصى لاعبين = 2 (لتسهيل الاختبار).

1. سجّل بالحساب الأول والثاني → كلاهما «مسجل» ويظهران في قائمة اللاعبين (2/2).
2. سجّل بالحساب الثالث → تظهر رسالة الاحتياط ويظهر في «قائمة الاحتياط» برقم 1.
3. انسحب بالحساب الأول (مقعد أساسي) → يُرقّى الحساب الثالث تلقائياً إلى اللاعبين الأساسيين.
4. انسحب بحساب احتياط → يُحذف من القائمة دون أي ترقية.
5. أنشئ مباراة أخرى بوقت متداخل وحاول التسجيل بحساب مسجل في الأولى → «أنت مسجل في مباراة أخرى في نفس الوقت».
6. أنشئ مباراة بوقت غير متداخل → التسجيل يعمل بشكل طبيعي.
7. ألغِ مباراة ثم حاول التسجيل فيها → «التسجيل مغلق لهذه المباراة».
8. اختبار السباق (اختياري): من جهازين في نفس اللحظة على آخر مقعد → واحد فقط يحصل على المقعد والآخر احتياط، ولا يتجاوز العدد الحد الأقصى أبداً.

---

## ملاحظة اللغة

واجهة التطبيق تتبع لغة الجهاز تلقائياً (العربية أو الإنجليزية حالياً).
النصوص في `app/lib/l10n/app_ar.arb` و `app_en.arb`، وتُولَّد أصناف الترجمة
تلقائياً عند البناء (لا حاجة لأي خطوة يدوية). لإضافة لغة جديدة يكفي إضافة
ملف `app_xx.arb` واحد.
