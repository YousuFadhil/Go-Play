# دليل الإعداد والتشغيل — Go Play (`v0.5.1-mvp`)

خطوات تشغيل التطبيق لأول مرة. خطوتان فقط تتطلبان تدخل مالك المنتج:
إنشاء مشروع Supabase، وتثبيت Flutter SDK.

---

## 1. إنشاء مشروع Supabase (مطلوب — يقوم به مالك المنتج)

1. سجّل حساباً في <https://supabase.com> (الخطة المجانية كافية).
2. أنشئ مشروعاً جديداً باسم `go-play`.
3. من **Authentication → Sign In / Providers → Email**:
   - تأكد أن مزوّد Email مفعّل (وهو الافتراضي).
   - **عطّل خيار "Confirm email"** (مهم: بدونه لن يحصل المستخدم على جلسة
     مباشرة بعد التسجيل، ولن تعمل اختبارات التكامل).
   - لا تفعّل مزوّد Phone (يتطلب Twilio — انظر DD-02).
4. من **SQL Editor** الصق `supabase/setup_all.sql` مرة واحدة — وهو تجميع
   لملفات الهجرة العشرة بالترتيب:

   | | |
   |---|---|
   | `0001_users.sql` | المستخدمون |
   | `0002_groups.sql` | الجداول الأساسية (باسمها القديم) |
   | `0003_matches.sql` | المباريات |
   | `0004_match_registrations.sql` | التسجيلات والاحتياط |
   | `0005_security_hardening.sql` | تشديد RLS |
   | `0006_match_management_v2.sql` | دورة حياة المباراة، السعة المشتقة، الإشعارات |
   | `0007_community_rename.sql` | `groups` → `communities`، و`member` → `player` |
   | `0008_role_based_authorization.sql` | `has_community_role` والدعوات |
   | `0009_community_management.sql` | نقل الملكية، إزالة عضو، حذف مجتمع |
   | `0010_invite_links.sql` | روابط الدعوة القابلة للمشاركة |

   > على مشروع `Go-Play` الحالي المخطط **مطبّق بالكامل** — لا حاجة لإعادة
   > تنفيذ أي SQL.

   > الملفات `0001`–`0006` تُنشئ الأسماء القديمة ثم يُعيد `0007` تسميتها.
   > هذا مقصود: تسلسل الهجرة يعكس تاريخ المشروع ولا يُعاد كتابته.
5. من **Project Settings → API** انسخ:
   - `Project URL`
   - `Publishable key` (أو `anon public key` في المشاريع الأقدم)

> ملاحظة أمنية: مفتاح `anon` مخصص للتطبيق وآمن للتضمين. مفتاح
> `service_role` لا يُستخدم في التطبيق إطلاقاً.

---

## 2. Flutter SDK

Flutter 3.44.7 مثبّت في `C:\Users\yousi\flutter`.

1. أضف `C:\Users\yousi\flutter\bin` إلى متغير PATH.
2. لتشغيل التطبيق على جهاز أو محاكي: ثبّت Android Studio ثم نفّذ
   `flutter doctor`.

`flutter analyze` و`flutter test` يعملان بدون Android Studio.

---

## 3. توليد ملفات المنصات (مرة واحدة)

مجلد `app/` يحتوي كود المشروع فقط:

```bash
cd app && flutter create . --org com.goplay --project-name go_play --platforms android,ios && flutter pub get
```

---

## 4. تشغيل التطبيق

**في PowerShell** (فاصل الأسطر هو ` وليس ^):

```powershell
flutter run `
  --dart-define=SUPABASE_URL=https://xxxx.supabase.co `
  --dart-define=SUPABASE_ANON_KEY=sb_publishable_...
```

أو في سطر واحد:

```bash
flutter run --dart-define=SUPABASE_URL=https://xxxx.supabase.co --dart-define=SUPABASE_ANON_KEY=sb_publishable_...
```

> علامة `^` تعمل في CMD فقط. لصق أمر بـ `^` داخل PowerShell يشغّل التطبيق
> **بدون الإعدادات** — وستظهر شاشة «Supabase configuration missing».

## بناء نسخة الإصدار

```bash
flutter build apk --release --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...
```

---

## 5. الاختبارات

راجع `Docs/12-Testing.md`. باختصار:

```bash
cd app && flutter test
```

اختبارات الوحدة فقط (74 ناجح، 5 متجاوَز). لتشغيل اختبارات التكامل الـ95
أضف نفس مفتاحي `--dart-define`.

---

## 6. اختبار يدوي سريع

1. **حساب**: أنشئ حساباً (الاسم، البريد، رقم جوال دولي مثل `9665xxxxxxxx`،
   كلمة مرور 8 أحرف فأكثر، المركز) → تظهر الشاشة الرئيسية مباشرة. أعد
   المحاولة بنفس البريد → «البريد الإلكتروني مسجل مسبقاً».
2. **مجتمع**: أنشئ مجتمعاً → تظهر فيه بشارة «المالك» ورمز الانضمام. من حساب
   آخر انضم بالرمز. جرّب رمزاً خاطئاً ثم رمز مجتمع أنت فيه بالفعل.
3. **الأدوار**: من حساب المالك ادعُ العضو الآخر أو غيّر دوره إلى مشرف →
   تظهر له صلاحيات إنشاء المباريات. اخفض دوره إلى لاعب → تختفي.
4. **مباراة**: أنشئ مباراة (الموقع، التاريخ، البداية والنهاية، عدد
   الأساسيين). جرّب نهاية قبل البداية أو تاريخاً ماضياً → رسالة خطأ. تظهر
   المباراة في تبويب المجتمع وفي «المباريات القادمة».
5. **التسجيل والاحتياط**: بمباراة أساسيوها 2 وثلاثة حسابات — الأول والثاني
   أساسيان، الثالث احتياط رقم 1. انسحب بالأول → يُرقّى الثالث تلقائياً
   ويصله إشعار.
6. **التداخل**: مباراة أخرى بوقت متداخل → «أنت مسجل في مباراة أخرى في نفس
   الوقت». بوقت غير متداخل → التسجيل يعمل.
7. **القفل**: بعد بداية المباراة المجدولة → لا تسجيل ولا انسحاب ولا تعديل.
8. **الإدارة**: من حساب المالك جرّب إزالة عضو (ينسحب من كل المباريات ويُرقّى
   الاحتياط)، ثم نقل الملكية (المالك السابق يصبح مشرفاً).

---

## ملاحظة اللغة

الواجهة عربية افتراضياً مع الإنجليزية متاحة. النصوص في
`app/lib/l10n/app_ar.arb` و`app_en.arb`، وتُولَّد أصناف الترجمة تلقائياً عند
البناء. لإضافة لغة يكفي ملف `app_xx.arb` واحد.
