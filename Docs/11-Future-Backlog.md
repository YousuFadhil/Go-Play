# 11 Future Backlog

أفكار وتحسينات مؤجلة — **لا تُنفَّذ في MVP** إلا بقرار صريح من مالك المنتج.

## مؤجل من نطاق المنتج الأصلي (خارج MVP بقرار PRD)

- توليد الفرق الذكي وتحريرها (Team Generator)
- محرك التقييمات (WIN/LOSS/GOAL/MVP)
- الإحصائيات ولوحات الصدارة
- الإشعارات (داخل التطبيق أو Push)
- إدخال نتائج المباريات والأهداف واختيار MVP

## قرارات هندسية قابلة للتطوير لاحقاً

- جدول `fields` للملاعب بدلاً من الموقع النصي (المسار موثق في تقرير Sprint 3)
- الاحتفاظ بسجل الانسحابات بحالة `withdrawn` بدلاً من الحذف (انظر DD-01)
- تقييد إنشاء المباريات بالمالك/المنظمين فقط (قرار منتج معلّق حتى التحقق مع مستخدمين حقيقيين)
- حماية تعديل `max_players` بعد وجود تسجيلات (حالياً واجهة التطبيق لا تتيح التعديل أصلاً)

## تحسينات تشغيلية

- نقل ملكية المجموعة، وإزالة الأعضاء من قبل المالك
- تعديل بيانات المباراة (الموقع/الوقت) مع إعادة التحقق من التداخل
- شاشة الملف الشخصي (تعديل الاسم والمركز)
- تحديث حالة المباراة تلقائياً إلى `completed` بعد انتهاء وقتها
- Realtime لتحديث القوائم لحظياً بدلاً من السحب للتحديث
- اختبارات تكامل آلية ضد Supabase محلي (Supabase CLI + Docker)
- ربط المستودع بـ GitHub (يتطلب تثبيت gh أو إضافة remote يدوياً)
