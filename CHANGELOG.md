# سجل التغييرات — Go Play

## v0.3.0-mvp — 2026-07-24

**V2 Match Management.** توسيع كامل لدورة حياة المباراة وإدارتها، معتمد
ومُختبر بالكامل (32/32 على الخادم الحي + اختبار دخان كامل على نسخة Release).

### التغييرات الرئيسية

- **دورة الحياة**: خُفضت حالات المباراة إلى ثلاث فقط: `open` / `full` /
  `completed`. أُزيلت Draft وCancelled وPostponed نهائياً من الكود
  والـ RPCs والترجمة.
- **القفل**: مشتق (زمني) وليس حالة مخزّنة — من وقت البداية المجدول حتى
  الاكتمال، يُمنع التسجيل والانسحاب والتعديل وإدارة القائمة.
- **الاكتمال**: مشتق أيضاً (الآن ≥ وقت النهاية) دون أي Scheduler أو
  pg_cron؛ يُحفظ بشكل كسول عند أول RPC يلمس الصف.
- **السعة**: المنظم يُدخل "اللاعبين الأساسيين" فقط؛ الحد الأقصى للتسجيل
  يُحسب تلقائياً = الأساسيون + إعداد عالمي `reserve_players` (افتراضي 6،
  جدول `app_settings` جديد).
- **الحذف**: أصبح غير مرتبط بالوقت — يمكن حذف أي مباراة غير مكتملة
  (سيُقيَّد لاحقاً فقط عند وجود بيانات تاريخية كنتائج/إحصائيات).
- **إدارة المباراة**: شاشة جديدة للمنظم (تعديل، إدارة الأساسيين/الاحتياط،
  إزالة لاعب، حذف).
- **الإشعارات**: نظام إشعارات داخل التطبيق جديد بالكامل (جدول
  `notifications`) — تصعيد من الاحتياط، إزالة، حذف مباراة، تعديل مباراة.
- **اللغة والواجهة**: العربية لغة افتراضية مع حفظ التفضيل، زر تبديل
  اللغة (عربي/إنجليزي) في شاشة الدخول، تحية بالاسم في الشاشة الرئيسية.
- **قاعدة البيانات**: Migration 0006 (`match_management_v2.sql`) مطبّقة
  على مشروع Supabase الحي.

## v0.2.1-mvp — 2026-07-22

إصلاحات ملاحظات المستخدمين (الجولة الأولى): رقم جوال عُماني (+968)،
إظهار المجموعات العامة، تصحيح حوار الانضمام للمجموعة.

## v0.2.0-mvp-baseline — 2026-07-21

النسخة المرجعية لإطلاق MVP. تم التحقق منها بالكامل: منطق الأعمال 33/33
اختباراً على الخادم الحي (شاملاً اختبار السباق على آخر مقعد)، وسيناريو
كامل من الواجهة (تسجيل، دخول، خروج) على المحاكي.

### الإصلاحات

- **إصلاح مشكلة التسجيل**: السبب الجذري كان تشغيل التطبيق بدون قيم
  `--dart-define`، فيفشل الاتصال قبل مغادرة الجهاز وتظهر رسالة عامة.
- **إصلاح إعدادات PowerShell**: أمر التشغيل في SETUP.md كان بصيغة CMD
  (`^`) ولا يعمل في PowerShell — الصيغة الصحيحة الآن موثقة.
- **Fail-Fast للإعدادات**: التطبيق لا يقلع بإعدادات ناقصة أو غير صالحة؛
  تظهر شاشة إرشادية ثنائية اللغة بالسبب وخطوات التشغيل الصحيحة، مع طباعة
  قيم التهيئة في وضع Debug (المفتاح مقنّع).
- **دعم INTERNET لنسخة Release**: أُضيفت الصلاحية للـ Manifest الرئيسي
  (قالب Flutter يضعها في debug/profile فقط).
- **تحسين رسائل الأخطاء**: رسالة مخصصة لأخطاء الشبكة، وتسجيل كامل لكل
  استثناء (النوع، الكود، التتبع) في سجل التشغيل بدل الرسائل العامة الصامتة.

### التغييرات السابقة ضمن هذه النسخة

- هوية الدخول: البريد الإلكتروني + كلمة المرور (DD-02)، والجوال حقل
  تواصل في الملف الشخصي.
- تقوية أمنية لدوال قاعدة البيانات (Migration 0005).

## v0.1.0-mvp — 2026-07-21

الأساس الأول: المصادقة، المجموعات، المباريات، التسجيل وقائمة الاحتياط
مع الترقية التلقائية ومنع التداخل الزمني — Flutter + Supabase بدون
خادم مخصص.
