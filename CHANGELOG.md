# سجل التغييرات — Go Play

## غير مُصدَر بعد

### هوية اللاعب في كل مكان (DD-15)

- **صورة إلى جانب كل اسم.** كل لاعب مجتمع مسجَّل يظهر الآن بصورته — أو أحرفه
  الأولى إن لم يضع صورة — في قائمة المباراة، وإدارة القائمة، وترتيب
  المشاركين، والفرق والملعب، وإدخال النتيجة، وقوائم الأعضاء.
- **الاسم يفتح الملف الشخصي**، مع بقاء قواعد خصوصية الملف كما هي: الخادم هو من
  يقرر ما إذا كان القارئ يستطيع فتحه.
- **الإجراءات الإدارية لم تُمَس.** حيث للصف إجراء — سحب، إزالة، تبديل، عدّاد
  أهداف — تكون الصورة والاسم هما زرّ الملف ويبقى الصف على ما كان.
- **اللاعب المحترف يبقى الاستثناء:** تمييز بصري واضح، وبلا ملف شخصي وبلا صورة.
- بلا أي تغيير في قاعدة البيانات، وبلا أي تغيير في BTGE.

### ترتيب المشاركين إدارياً (Migration 0053)

- **تحكّم كامل للمالك والمشرف في قائمة المشاركين.** شاشة جديدة «ترتيب
  المشاركين» تعرض الأساسيين والاحتياط معاً: سحب المقبض لإعادة الترتيب داخل
  القائمة، وسحب مشارك على آخر — أو الضغط على الاثنين — للتبديل بينهما.
- **ترتيب التسجيل يبقى الافتراضي.** لا يتغيّر شيء في مباراة لم يُرتّبها أحد.
  أول عملية ترتيب أو تبديل تُفعّل الترتيب الإداري لبقية عمر المباراة، ولا
  رجوع بعدها.
- **اللاعب المحترف الذي يضعه المشرف ضمن الأساسيين يبقى أساسياً** — لا يزيحه
  لاعب مجتمع ينتظر في الاحتياط، ولا تسجيل جديد، ولا انسحاب من الأساسيين؛
  المقعد الشاغر يُملأ من رأس الاحتياط بالترتيب المعتمد.
- **السعة محفوظة بالتصميم:** لا تُرسل خانة «أساسي» أبداً، بل تُشتق من الموضع،
  فلا يمكن لأي عملية أن تتجاوز `starting_players`.
- **المباريات المنتهية تبقى قابلة للإدارة**، مع بقاء قائمة الأساسيين سجلاً
  لمن لعب فعلاً.
- **BTGE لم يُمَس.** المحترفون خارج المحرك كما هم، وتغيير مجموعة لاعبي
  المجتمع الأساسيين يُلغي التشكيلة المخزّنة عبر الآلية المعتمدة سابقاً.
- التفاصيل في `Docs/10-Design-Decisions.md` (DD-14) و`Docs/07-Database-Design.md` §7.

## v0.4.0-public-beta — 2026-08-09

**The first public beta.** Go Play opens to visitors: the app now has a public
front door, communities are the place everything happens, and the whole
interface has been redrawn around both.

### Discover, and browsing without an account

- **A public Discover homepage.** Opening the app now shows the communities and
  matches that are open to everyone, rather than a login screen.
- **Guest browsing.** A visitor can look around — browse communities, read
  match details, see who is playing — before deciding to sign up. Signing in is
  asked for at the point it is actually needed, and not before.
- **Public view security hardening.** What a guest can see is defined
  deliberately and narrowly: public reads expose only the fields meant to be
  public, and everything else stays behind membership as it always did.

### Community-first experience

- The community is the centre of the app. Joining, members, matches, results
  and standings are all reached from the community itself, with one join code
  and one join policy per community.

### Matches and results

- **The best player is now optional.** A match result can be recorded without
  naming an MVP, and one can be named later without redoing anything.
- **Team formation visualization.** Generated teams are shown on a pitch, in
  formation, so a line-up can be read at a glance instead of as a list.

### Interface

- **A redesigned interface** across the app: clearer screens, consistent
  navigation, and loading states that show the shape of what is coming.
- **A redesigned profile** bringing a player's rating, statistics and details
  together in one place.
- **A shared design system.** One set of colours, type, spacing and components
  used by every screen, so the app looks and behaves the same throughout.
- **12-hour localized time.** Times read the way they are spoken — 12-hour with
  a localized AM/PM — in both Arabic and English.

## v0.6.0-mvp — 2026-07-27

**تبسيط Community-first، سياسة الانضمام، ودور مدير النظام.**

### تغييرات قاطعة

- أُعيد توليد كل رموز الانضمام بطول 12 حرفاً؛ أي رمز من ستة أحرف شورك
  سابقاً لم يعد يعمل.
- أُزيلت الدعوات الموجّهة المعلّقة مع جدولها؛ من لم يقبل دعوته ينضم الآن
  بالرابط أو بالرمز.
- روابط الدعوة القديمة (`goplay://invite/<token>`) لم تعد تُفهم؛ الشكل
  الجديد `goplay://join/<code>`.

### أُزيل

نظاما الدعوة كلاهما: جدول `invitations` ودوالّه الثلاث وشاشتا «دعوة لاعب»
و«دعواتي»، وجدول `community_invite_links` وأربع دوال ومساعد وشاشة إدارة
الروابط، والرابط المرتبط بمباراة. لم تكن هناك «دعوة إلى مباراة» أصلاً:
التسجيل كان ذاتياً منذ البداية وبقي كذلك.

### أُضيف

- عنوان المباراة إجباري عند الإنشاء والتعديل (`matches.title` NOT NULL).
- رمز الانضمام هو المعرّف الوحيد: الرابط والرمز يحملانه نفسه.
- شاشة دعوة المجتمع: مشاركة الرابط، نسخ الرابط، نسخ الرمز.
- `preview_community_invite` — الدالة الوحيدة المتاحة لغير المسجَّلين.
- **تجديد رمز الانضمام** (للمالك والمشرف) من شاشة الدعوة: يُبطل الرابط
  والرمز الحاليين فوراً، ويعرض الجديدين في مكانهما فيمكن نسخهما أو
  مشاركتهما دون إعادة فتح الشاشة. الأعضاء والمباريات والتسجيلات لا تتأثر.

### سياسة الانضمام (بدل الخصوصية)

- **المجتمع يُرى دائماً.** الظهور والانضمام سؤالان مختلفان، والإعداد الوحيد
  الآن `join_policy`: `OPEN` (ينضم أي أحد من القائمة) أو `CODE_REQUIRED`
  (يظهر ويُفتح، لكن الانضمام يحتاج الرمز). الافتراضي `OPEN`.
- رابط الدعوة لم يتغيّر ويعمل في الحالتين — يحمل الرمز، والرمز مقبول أياً
  كانت السياسة.
- **تغيير قاطع:** أُزيل العمود `is_private`. المجتمعات الخاصة الثلاثة صارت
  `CODE_REQUIRED`: طريقة الانضمام محفوظة، لكن أسماءها صارت مرئية للجميع.
  هذا يعكس DD-11 عن قصد.

### مدير النظام

- جدول `system_admins(user_id)` — دور مستقل تماماً عن أدوار المجتمع،
  يُمنح بـ SQL يدوياً فقط. لا RPC ولا شاشة لمنحه، والجدول بلا أي سياسة RLS
  فلا يُقرأ من العميل، ولا يستطيع التطبيق حذف حساب مدير نظام.
- ست دوال محميّة بـ `is_system_admin()`: عرض وبحث وحذف للمستخدمين
  والمجتمعات والمباريات.
- شاشة واحدة بثلاثة تبويبات خلف أيقونة لا يراها غير مدير النظام. بلا لوحات
  ولا تحليلات ولا أدوات إشراف.
- **الحذف يعيد استخدام المسارات القائمة:** استُخرجت `purge_community`
  و`purge_match` و`purge_membership` من أجساد الدوال الثلاث القائمة، وصارت
  تلك تفوّض إليها بعد فحص الصلاحية. فحذف مستخدم يُرقّي الاحتياط تماماً كما
  تفعل إزالة عضو — مُتحقَّق منه ميدانياً.

### إصلاح

`delete_community` كان يحذف من جدول أُسقط، فتعطّل حذف المجتمع في الإنتاج.
أُصلح في 0014. راجع DD-12.

## v0.5.1-mvp — 2026-07-26

**تحسينات صغيرة: الأدوار، الخصوصية، الوقت، بطاقة المباراة.**
بلا أي تغيير في قاعدة البيانات — لا هجرة ولا دوال ولا سياسات.

### إدارة أدوار الأعضاء

مطابِقة للمطلوب ومنفَّذة سلفاً منذ `v0.4.0-mvp`: المالك يرقّي لاعباً إلى
مشرف ويُنزل المشرف إلى لاعب ويزيل الاثنين، ولا يستطيع تنزيل نفسه أو إزالتها؛
المشرف يزيل اللاعبين فقط ولا يغيّر أي دور ولا يمسّ مشرفاً أو مالكاً؛ اللاعب
بلا صلاحيات. أُضيف الاختبار الناقص الوحيد (تنزيل مشرف إلى لاعب). لم يتغيّر
أي كود أو قاعدة عمل.

### المجتمع خاص افتراضياً

- المجتمع الجديد يُنشأ **خاصاً** ما لم يقرر المنظم غير ذلك.
- مفتاح **مجتمع خاص** داخل قائمة المجتمع (للمالك فقط) لتبديل الظهور. التبديل
  تحديث مباشر تحكمه سياسة `communities_update_owner` القائمة — بلا دالة
  جديدة وبلا هجرة وبلا شاشة إضافية.
- سلوك روابط الدعوة لم يتغيّر: الرابط يعمل في الحالتين (مُختبَر).

### صيغة الوقت

- أُزيلت الصيغة الثابتة (24 ساعة). الوقت يتبع تفضيل الجهاز عبر
  `TimeOfDay.format`، فيظهر `8:30 PM` أو `٨:٣٠ م` على جهاز 12 ساعة و`20:30`
  على جهاز 24 ساعة — بلا صيغة مثبَّتة في الكود.
- أُصلح انعكاس النطاق الزمني في العربية عالمياً: النطاق يُلفّ بعلامتَي عزل
  اتجاهي في مكان واحد (`core/time_format.dart`) يستخدمه كل عرض للوقت، فلم
  تعد المباراة تبدو منتهية قبل أن تبدأ.

### بطاقة المباراة

اسم المباراة أصبح العنوان الأساسي (عريض، سطر واحد، يُقصّ بنقاط عند الطول)،
والموقع تحته كمعلومة ثانوية، ثم التاريخ والوقت ثم عدد اللاعبين
الأساسيين فقط (مثال: `👥 6 لاعبًا`) — وهو عدد من يلعب فعلاً، لا
`max_registration` الذي يشمل الاحتياط (DD-06). بلا احتياط ولا عدد مسجَّلين
ولا مقاعد متبقية. المباراة بلا اسم تبقى تعرض موقعها عنواناً، وعندها لا يُكرَّر
الموقع.
تغيير عرض فقط: لا قاعدة بيانات ولا منطق ولا مستودع.

### الاختبارات

169 اختباراً (74 وحدة + 95 تكامل): 5 اختبارات وحدة جديدة لصيغة الوقت
(تفضيلا الجهاز، والعزل الاتجاهي)، و5 اختبارات تكامل جديدة (تنزيل مشرف،
تبديل الظهور، منع المشرف واللاعب منه، وبقاء رابط الدعوة عاملاً عبر التبديل).

## v0.5.0-mvp — 2026-07-26

**Smart Community Invitation — دعوة ذكية للمجتمع والمباراة.** رابط دعوة واحد يقود اللاعب من الرابط إلى
تفاصيل المباراة: يفتح الرابط فيرى ما هو مدعوّ إليه قبل أن يملك حساباً، ثم
ينضم إلى المجتمع ويُسجَّل في المباراة تلقائياً.

### نوعان من الدعوة

- **مجتمع فقط**: ينضم ثم ينتقل إلى المجتمع. لا ينتهي حتى يُلغيه مشرف.
- **مجتمع + مباراة**: ينضم ويُسجَّل ثم ينتقل مباشرة إلى تفاصيل المباراة.
  ينتهي ببدء المباراة أو بحذفها.

الاستبدال يمنح دور اللاعب فقط، دائماً.

### الانضمام والتسجيل نتيجتان منفصلتان

إذا نجح الانضمام وفشل التسجيل **لا تُلغى العضوية**: يبقى المستخدم عضواً،
ويُعرض السبب الحقيقي (المباراة ممتلئة، التسجيل مغلق، تعارض وقت)، ثم ينتقل
إلى المجتمع. التسجيل التلقائي يمرّ بـ `register_for_match` دون تعديل، فيخضع
لكل قواعد التسجيل القائمة — السعة وقائمة الاحتياط والترقية التلقائية ومنع
التداخل والقفل. إن كان مكانه في الاحتياط، فالاحتياط.

### قاعدة البيانات

- **Migration 0010** — جدول جديد `community_invite_links` وأربع دوال:
  `create_invite_link`، `revoke_invite_link`، `preview_invite_link`،
  `redeem_invite_link`، إضافة إلى المساعد `invite_link_state`.
- إضافة صرفة: لم يُعدَّل أي جدول أو سياسة أو دالة قائمة. جدول `invitations`
  (الدعوة الموجّهة) لم يُمسّ — راجع DD-10 لسبب فصل الكيانين.
- `preview_invite_link` هي الدالة الوحيدة الممنوحة لـ `anon`، لأن المعاينة
  يجب أن تعمل قبل إنشاء الحساب. تُرجع أعمدة ثابتة لا تشمل `join_code` ولا
  قائمة اللاعبين ولا منشئ الرابط، والرابط الملغى لا يُرجع شيئاً سوى حالته.

### التطبيق

- شاشة دعوة جديدة تعمل بدون تسجيل دخول؛ من يسجّل الدخول منها يعود إلى الدعوة
  نفسها بدل أن يفقدها.
- استقبال الروابط عبر `goplay://invite/<token>` (Intent Filter + معالجة
  مدمجة في Flutter، بلا حزم جديدة)، مع حقل لصق للرابط لأن بعض تطبيقات
  المراسلة لا تجعل الروابط ذات النطاق الخاص قابلة للنقر.
- مشاركة الدعوة من شاشة المجتمع (للمالك والمشرف) ومن شاشة إدارة المباراة.
- شاشة إدارة الروابط: قائمة بالروابط الحيّة مع نسخ وإلغاء، وتمييز الرابط
  الذي انتهى ببدء المباراة أو بحذفها.
- الشاشة تقول صراحة إن المقاعد الأساسية مكتملة **قبل** الضغط، لا بعده.

### الاختبارات

159 اختباراً (69 وحدة + 90 تكامل)، منها 29 اختباراً جديداً لروابط الدعوة
تغطي النوعين، والمعاينة بدون حساب، والنتيجة المنفصلة عند فشل التسجيل، وكل
طريقة يتوقف بها الرابط عن العمل. تم التحقق كذلك يدوياً على نسخة Release من
الرابط حتى شاشة المباراة.

## v0.4.0-mvp — 2026-07-26

**Community-first Architecture Migration.** انتقال معماري كامل من نموذج
Groups-first إلى Community-first، مع استبدال الصلاحيات المبنية على الملكية
بصلاحيات مبنية على الدور. نُفِّذ على سبع مراحل، كل مرحلة مطبَّقة ومُتحقَّق
منها على قاعدة البيانات الحية قبل اعتمادها (110/110 اختبار، ونسخة Release
مثبَّتة ومختبَرة مقابل الإنتاج).

### تغيير قاطع

لا توجد طبقة توافق. قاعدة البيانات المُرحَّلة والتطبيق الحالي هما التركيبة
الوحيدة المدعومة — أي نسخة APK سابقة للترحيل لن تعمل.

### قاعدة البيانات

- **Migration 0007** — إعادة التسمية: `groups` → `communities`،
  `group_members` → `community_members`، `group_id` → `community_id`،
  والدور `member` → `player`. أُعيدت تسمية 4 فهارس وTrigger و11 قيداً
  بأسمائها الفعلية المقروءة من `pg_constraint`، وأُعيد إنشاء 7 سياسات
  بنفس قواعدها.
- **Migration 0008** — الصلاحيات المبنية على الدور: الدالة
  `has_community_role(community_id, user_id, min_role)` أصبحت المحدِّد
  الوحيد للصلاحية، والأدوار تراكمية (owner ≥ admin ≥ player). أُعيد توجيه
  كل السياسات إليها، واستُبدلت فحوص `created_by <> auth.uid()` داخل الـ
  RPCs بفحص الدور (`NOT_AUTHORIZED`). أُضيف جدول `invitations` مع
  `create_invitation` و`revoke_invitation` و`accept_invitation`
  و`set_member_role`.
- **Migration 0009** — إدارة المجتمع: `transfer_ownership` (ذرية، تُبقي
  مالكاً واحداً دائماً)، `remove_member` (تسحب العضو من كل مباريات
  المجتمع وتُرقّي الاحتياط)، `delete_community` (حذف متسلسل شامل).
- `owner_id` أصبحت قيمة مشتقة للعرض فقط، و`created_by` للتدقيق فقط —
  لا يُقرأ أيٌّ منهما لمنح صلاحية أو منعها.

### التطبيق

- بنية المزايا أصبحت `communities` / `members` / `invitations` /
  `matches` / `auth` / `home` / `notifications`.
- `GroupService` قُسِّم إلى `CommunityRepository` و`MemberRepository`
  و`InvitationRepository`؛ ولم تعد أي شاشة تتصل بـ Supabase مباشرة، ولا
  تستورد أي شاشة حزمة `supabase_flutter`.
- واجهات جديدة: الدعوات، إدارة الأعضاء وأدوارهم، نقل الملكية، حذف
  المجتمع. عناصر الإدارة تُقيَّد بالدور مع رسالة مترجمة تشرح سبب المنع
  بدل الإخفاء الصامت.
- أخطاء المصادقة أصبحت نوعاً خاصاً بالمجال (`AuthFailureException`) بدل
  تسريب أنواع الحزمة إلى الشاشات.
- حُذفت 12 مفتاح ترجمة غير مستخدمة من الملفين العربي والإنجليزي.

### الاختبارات

65 اختبار تكامل بلغة Dart تعمل ضد مشروع Supabase حقيقي، تغطي مصفوفة
الصلاحيات المعتمدة، وقواعد العمل DD-01–DD-08، ودورة حياة الدعوات، ونقل
الملكية، وإزالة الأعضاء، وترقية الاحتياط، وحالات السباق. تُتجاوَز تلقائياً
بدون بيانات اعتماد، فيبقى `flutter test` صالحاً للجميع. راجع
`Docs/12-Testing.md`.

### التوثيق

حُدِّثت وثائق المشروع (PRD, SRS, Technical Architecture, ERD v3, Database
Design v3, UI/UX) إلى نموذج Community-first، وأُضيف `Docs/12-Testing.md`،
وسُجِّل القرار DD-09، ووُسِمت الوثائق التاريخية بوضوح.

### فجوة معروفة

مغادرة المجتمع (PD-12) منفَّذة ومختبَرة في قاعدة البيانات لكن بلا واجهة
في التطبيق. إضافتها قرار منتج — راجع `Docs/11-Future-Backlog.md`.

## v0.3.0-mvp — 2026-07-24

**V2 Match Management.** توسيع كامل لدورة حياة المباراة وإدارتها، معتمد
ومُختبر بالكامل (32/32 على الخادم الحي + اختبار دخان كامل على نسخة Release).

### التغييرات الرئيسية

- **دورة الحياة**: خُفضت حالات المباراة إلى ثلاث فقط: `open` / `full` /
  `completed`. أُزيلت Draft وCancelled وPostponed نهائياً من الكود
  والـ RPCs والترجمة.
- **القفل**: مشتق (زمني) وليس حالة مخزّنة — من وقت البداية المجدول حتى
  الاكتمال، يُمنع التسجيل والانسحاب والتعديل وإدارة القائمة.
- **الاكتمال**: مشتق أيضاً (الآن ≥ وقت النهاية) دون أي Scheduler أو
  pg_cron؛ يُحفظ بشكل كسول عند أول RPC يلمس الصف.
- **السعة**: المنظم يُدخل "اللاعبين الأساسيين" فقط؛ الحد الأقصى للتسجيل
  يُحسب تلقائياً = الأساسيون + إعداد عالمي `reserve_players` (افتراضي 6،
  جدول `app_settings` جديد).
- **الحذف**: أصبح غير مرتبط بالوقت — يمكن حذف أي مباراة غير مكتملة
  (سيُقيَّد لاحقاً فقط عند وجود بيانات تاريخية كنتائج/إحصائيات).
- **إدارة المباراة**: شاشة جديدة للمنظم (تعديل، إدارة الأساسيين/الاحتياط،
  إزالة لاعب، حذف).
- **الإشعارات**: نظام إشعارات داخل التطبيق جديد بالكامل (جدول
  `notifications`) — تصعيد من الاحتياط، إزالة، حذف مباراة، تعديل مباراة.
- **اللغة والواجهة**: العربية لغة افتراضية مع حفظ التفضيل، زر تبديل
  اللغة (عربي/إنجليزي) في شاشة الدخول، تحية بالاسم في الشاشة الرئيسية.
- **قاعدة البيانات**: Migration 0006 (`match_management_v2.sql`) مطبّقة
  على مشروع Supabase الحي.

## v0.2.1-mvp — 2026-07-22

إصلاحات ملاحظات المستخدمين (الجولة الأولى): رقم جوال عُماني (+968)،
إظهار المجموعات العامة، تصحيح حوار الانضمام للمجموعة.

## v0.2.0-mvp-baseline — 2026-07-21

النسخة المرجعية لإطلاق MVP. تم التحقق منها بالكامل: منطق الأعمال 33/33
اختباراً على الخادم الحي (شاملاً اختبار السباق على آخر مقعد)، وسيناريو
كامل من الواجهة (تسجيل، دخول، خروج) على المحاكي.

### الإصلاحات

- **إصلاح مشكلة التسجيل**: السبب الجذري كان تشغيل التطبيق بدون قيم
  `--dart-define`، فيفشل الاتصال قبل مغادرة الجهاز وتظهر رسالة عامة.
- **إصلاح إعدادات PowerShell**: أمر التشغيل في SETUP.md كان بصيغة CMD
  (`^`) ولا يعمل في PowerShell — الصيغة الصحيحة الآن موثقة.
- **Fail-Fast للإعدادات**: التطبيق لا يقلع بإعدادات ناقصة أو غير صالحة؛
  تظهر شاشة إرشادية ثنائية اللغة بالسبب وخطوات التشغيل الصحيحة، مع طباعة
  قيم التهيئة في وضع Debug (المفتاح مقنّع).
- **دعم INTERNET لنسخة Release**: أُضيفت الصلاحية للـ Manifest الرئيسي
  (قالب Flutter يضعها في debug/profile فقط).
- **تحسين رسائل الأخطاء**: رسالة مخصصة لأخطاء الشبكة، وتسجيل كامل لكل
  استثناء (النوع، الكود، التتبع) في سجل التشغيل بدل الرسائل العامة الصامتة.

### التغييرات السابقة ضمن هذه النسخة

- هوية الدخول: البريد الإلكتروني + كلمة المرور (DD-02)، والجوال حقل
  تواصل في الملف الشخصي.
- تقوية أمنية لدوال قاعدة البيانات (Migration 0005).

## v0.1.0-mvp — 2026-07-21

الأساس الأول: المصادقة، المجموعات، المباريات، التسجيل وقائمة الاحتياط
مع الترقية التلقائية ومنع التداخل الزمني — Flutter + Supabase بدون
خادم مخصص.
